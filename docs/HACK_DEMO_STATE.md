# HACK-DEMO recording handoff

Updated September 3, 2026. Owner: [HACK-DEMO #74](https://github.com/Jainil2/scopedelta/issues/74).

## Current outcome

**Script v2 is prepared. The final video is not complete.** This continuation prepares the script; it has not created a fresh production workspace, provisioned a judge, changed Netlify configuration, sent invitations, recorded footage, or generated replacement narration.

The founder reports that PR #77 is merged, all tools are available, and the three demo environment values have been added in Netlify. GitHub confirms [PR #77](https://github.com/Jainil2/scopedelta/pull/77) merged on September 3 at 08:32 UTC, as `a44b619153df508d8cc91e8a1a0f678524974919`. This script was prepared against that exact source. Current protected environment values and fresh native-browser tool execution were not inspected in this continuation.

The founder's current direction supersedes the old four-tool/seeded-only script: demonstrate a first user creating a workspace, client and project, then agreement → client request → drift → human-reviewed commercial treatment → delivery acceptance → project completion. Existing product rules and human confirmations remain intact. No product-code change is part of this work.

## Prepared artifacts

- [HACK_DEMO.md](HACK_DEMO.md): v2, nine scenes, 2:50 edit target, 322 spoken words. Includes environment answer, agent prompts, tool mapping, actor boundaries and a truthful internal-only fallback.
- [HACK_DEMO_RUNBOOK.md](HACK_DEMO_RUNBOOK.md): ordered tool inputs, returned-ID dependencies, baseline evidence offsets, confirmed impact/packet requirements, client approval and acceptance, actual review evidence and completion verification.
- [WEBMCP_SUBMISSION_DRAFT.md](WEBMCP_SUBMISSION_DRAFT.md): current pitch and recording pointers aligned with v2; the August 31 release evidence remains explicitly historical.
- `output/hack-demo/HACK-DEMO-narration-draft.wav`: existing **obsolete v1** voice track, 168 seconds, 48 kHz mono PCM, previously decoded and measured at -1.5 dB peak. Do not use it with v2 scenes.
- `output/hack-demo/narration-draft.txt` and ignored `.cache/hack-demo/` segments/manifests: existing v1 working assets, preserved. Private configuration is not a script or publication asset.

FFmpeg and the installed macOS Samantha voice produced the earlier draft. Replacement narration, actual product capture, video edit and final caption timing remain pending. No `HACK-DEMO.mp4` or final `.srt` exists yet.

Script validation: all 55 JSON call examples across 25 tool names passed the merged tool schemas with synthetic placeholder IDs; the two evidence text ranges match the source. Local document links resolve, and the nine scene allocations are continuous and total 170 seconds. This was a local contract/document check with no API execution, not a production rehearsal or a measured voice-track duration.

## Configuration and access

No extra Netlify variable is needed for the main v2 story. `WEBMCP_DEMO_ENABLE`, `WEBMCP_DEMO_JUDGE_EMAIL` and `WEBMCP_DEMO_JUDGE_PASSWORD` configure the optional provisioning command; setting them does not itself run that command. A fresh workspace created by a verified user needs no fixture seed. The browser agent does not need an app-side AI provider key; built-in AI analysis has separate configuration and is outside the timed cut.

The complete client round trip needs a separate verified external **approver** session. The internal client-account record created during setup is not that session. Arrange the ordinary private invitation after the new project exists; a new signup or emailed invitation depends on existing email configuration. Keep recipient selection, credentials and private invitation links outside recorded footage. No additional demo env value substitutes for this access.

Before the workflow expansion, the founder's signed-in in-app browser showed their personal workspace with no clients/projects and no accessible NOVA fixture. Read-only original-tool attempts then returned empty assigned work / no accessible NOVA. The prior handoff's statement that no tool had ever been executed was too broad. Those older observations do not establish the current production data state; inspect it before rehearsal.

The in-app browser is the preferred available recording surface. Earlier Chrome diagnostics reported the required extension connection unavailable; Brave had no open ScopeDelta tab. Do not assume either has since become ready. Use existing verified sessions through supported browser tooling, without reading credential stores.

## Next recording steps

1. Inspect current browser state and native tools. Select the Team session and arrange a genuine external approver session. The first-user story can use a fresh owner workspace; do not silently switch to the member-only seeded judge's workspace for client/project creation.
2. Rehearse the runbook once and retain real ordinary-UI evidence. The demo engagement produces a scope-review checklist, not newly built software. Keep the manager's illustrative USD 250 quote, client approval, and delivery acceptance distinct.
3. Capture the before/after drift ledger while work remains active. Completing items can remove them from the active ledger; that alone does not prove a commercial link was repaired.
4. Correct narration to match observed results. Capture the actual checklist review and its findings before recording passed verification, done work or an accepted deliverable. If no client session is available, prepare internal shots and label a draft as awaiting approval; do not claim the full journey is complete.
5. Produce replacement v2 narration, edit to 2:50, synchronize captions and review the export for readable UI, truthful state transitions and intelligible audio. Keep credentials, private emails, tokens and opaque IDs out of footage.
6. Save `HACK-DEMO.mp4` at the repository root and `HACK-DEMO.srt`, then record the actual location in #74. Public upload, repository visibility/license decisions and submission remain separate founder actions.

Existing untracked media/output files were preserved. The script work uses branch `codex/hack-demo-script-v2`; the linked documentation PR is the review artifact. Do not close #74 on script preparation alone: its final narrated-video acceptance criteria remain outstanding.
