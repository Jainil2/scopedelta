# HACK-DEMO recording handoff

Updated September 3, 2026. Owner: [HACK-DEMO #74](https://github.com/Jainil2/scopedelta/issues/74).

## Current outcome

**Script v2 and replacement narration are prepared; real UI capture has started. The final video is not complete.** The external Approver invitation is accepted and both account sessions are usable. Native WebMCP execution is blocked by the browser's configuration limit, despite the app displaying 59 active tools.

### Live recording checkpoint

- **NOVA Demo Studio**, **NOVA Retail**, and **NOVA — Launch scope review** (`NOVA`) already exist. Their creation happened through ordinary UI before footage capture; do not present it as captured native WebMCP execution.
- The external client invitation is **accepted**, verified through active Approver access in the separate account and the main account's client-collaboration view. No new workspace, invitation or environment variable is needed for this account. Keep private email, credentials and invitation tokens out of repository evidence and footage.
- The main in-app browser tab is on NOVA Commercial. The separate client session is connected in Brave on NOVA's client project page. Use the existing sessions; do not read credential stores.
- Preserved **NOVA review agreement — demo** as parsed, ready private evidence. Created **Initial baseline**, currently draft. Verified two scope items: the retail-review deliverable and the wholesale-discount exclusion, each anchored to the 116-character source. Do not add duplicates. No baseline activation, request, decision, delivery or acceptance is confirmed yet.
- Native browser tool discovery fails with: **WebMCP is disabled for this page because the site’s WebMCP configuration exceeds supported limits. After correcting the configuration, reset NodeREPL and refresh the browser tab to retry.** The UI's 59-tool badge does not establish that the browser can execute them. Source registers the whole workspace catalog at once. The numerical browser limit is unknown.
- The founder explicitly approved the narrow compatibility fix, recorded in [#74](https://github.com/Jainil2/scopedelta/issues/74#issuecomment-5523884838). The local change keeps ten base workspace tools and loads one business workflow through discover_workflows.load, replacing the previous selection. It awaits review and deployment; do not claim production execution is fixed yet.
- Local native-browser compatibility was checked with a compiled diagnostic importing the real registration and catalog code. Native discovery accepted the ten base tools, loaded delivery_work (the largest workflow schema), then replaced it with project_templates (the next largest), leaving eleven tools and zero registration failures. This verifies registration/selection, not authenticated API execution or production deployment. The diagnostic uses no API mocks and is not demo footage.
- Real selected-tab screenshot sequences are saved under `output/hack-demo/v2/`: overview, source preservation, baseline creation and retail-scope entry. The initial `00-capture-check` probe failed and is not usable; later captures report no capture error. Each clip has a frame/timestamp manifest. These are ordinary UI actions, not WebMCP executions. No final video has been encoded yet.
- Nine replacement Samantha narration AIFF segments exist under `output/hack-demo/v2/narration/`, with measured durations. They still need reconciliation with observed scenes, editing and captions. Do not narrate future workflow outcomes as completed facts.
- Founder reports unreadable text and inconsistent UI. **Finish the demo before the UI redesign.** The narrow WebMCP recording blocker is separate from that redesign.

The founder reports that PR #77 is merged, all tools are available, and the three demo environment values have been added in Netlify. GitHub confirms [PR #77](https://github.com/Jainil2/scopedelta/pull/77) merged on September 3 at 08:32 UTC, as `a44b619153df508d8cc91e8a1a0f678524974919`. This script was prepared against that exact source. Protected environment values were not inspected. Native production discovery failed as recorded above; local compatibility evidence is separate.

The founder's current direction supersedes the old four-tool/seeded-only script: demonstrate a first user creating a workspace, client and project, then agreement → client request → drift → human-reviewed commercial treatment → delivery acceptance → project completion. Existing product rules and human confirmations remain intact. The only product-code scope added by founder approval is the registration compatibility fix; the UI redesign remains deferred.

## Prepared artifacts

- [HACK_DEMO.md](HACK_DEMO.md): v2, nine scenes, 2:50 edit target, 322 spoken words. Includes environment answer, agent prompts, tool mapping, actor boundaries and a truthful internal-only fallback.
- [HACK_DEMO_RUNBOOK.md](HACK_DEMO_RUNBOOK.md): ordered tool inputs, returned-ID dependencies, baseline evidence offsets, confirmed impact/packet requirements, client approval and acceptance, actual review evidence and completion verification.
- [WEBMCP_SUBMISSION_DRAFT.md](WEBMCP_SUBMISSION_DRAFT.md): current pitch and recording pointers aligned with v2; the August 31 release evidence remains explicitly historical.
- `output/hack-demo/HACK-DEMO-narration-draft.wav`: existing **obsolete v1** voice track, 168 seconds, 48 kHz mono PCM, previously decoded and measured at -1.5 dB peak. Do not use it with v2 scenes.
- `output/hack-demo/narration-draft.txt` and ignored `.cache/hack-demo/` segments/manifests: existing v1 working assets, preserved. Private configuration is not a script or publication asset.

FFmpeg and the installed macOS Samantha voice produced the earlier draft. Replacement narration segments and initial product footage now exist; the complete workflow capture, video edit and final caption timing remain pending. No `HACK-DEMO.mp4` or final `.srt` exists yet.

Script validation: all 55 JSON call examples across 25 tool names passed the merged tool schemas with synthetic placeholder IDs; the two evidence text ranges match the source. Local document links resolve, and the nine scene allocations are continuous and total 170 seconds. This was a local contract/document check with no API execution, not a production rehearsal or a measured voice-track duration.

## Configuration and access

No extra Netlify variable is needed for the main v2 story. `WEBMCP_DEMO_ENABLE`, `WEBMCP_DEMO_JUDGE_EMAIL` and `WEBMCP_DEMO_JUDGE_PASSWORD` configure the optional provisioning command; setting them does not itself run that command. A fresh workspace created by a verified user needs no fixture seed. The browser agent does not need an app-side AI provider key; built-in AI analysis has separate configuration and is outside the timed cut.

The complete client round trip uses the now-verified separate external **approver** session. The internal client-account record created during setup is not that session. Arrange the ordinary private invitation after the new project exists; a new signup or emailed invitation depends on existing email configuration. Keep recipient selection, credentials and private invitation links outside recorded footage. No additional demo env value substitutes for this access.

Before the workflow expansion, the founder's signed-in in-app browser showed their personal workspace with no clients/projects and no accessible NOVA fixture. Read-only original-tool attempts then returned empty assigned work / no accessible NOVA. The prior handoff's statement that no tool had ever been executed was too broad. Those older observations do not establish the current production data state; inspect it before rehearsal.

The in-app browser is the main-account recording surface. Brave now has a connected, verified external Approver session. Selected-tab screenshot sequences work through supported browser tooling; full desktop capture is unnecessary.

## Next recording steps

1. Inspect current browser state and native tools. Select the Team session and arrange a genuine external approver session. The first-user story can use a fresh owner workspace; do not silently switch to the member-only seeded judge's workspace for client/project creation.
2. Rehearse the runbook once and retain real ordinary-UI evidence. The demo engagement produces a scope-review checklist, not newly built software. Keep the manager's illustrative USD 250 quote, client approval, and delivery acceptance distinct.
3. Capture the before/after drift ledger while work remains active. Completing items can remove them from the active ledger; that alone does not prove a commercial link was repaired.
4. Correct narration to match observed results. Capture the actual checklist review and its findings before recording passed verification, done work or an accepted deliverable. If no client session is available, prepare internal shots and label a draft as awaiting approval; do not claim the full journey is complete.
5. Produce replacement v2 narration, edit to 2:50, synchronize captions and review the export for readable UI, truthful state transitions and intelligible audio. Keep credentials, private emails, tokens and opaque IDs out of footage.
6. Save `HACK-DEMO.mp4` at the repository root and `HACK-DEMO.srt`, then record the actual location in #74. Public upload, repository visibility/license decisions and submission remain separate founder actions.

Existing untracked media/output files were preserved. The script and narrowly approved registration fix use branch `codex/hack-demo-script-v2`; PR #78 is the review artifact. Do not close #74 on script preparation alone: its final narrated-video acceptance criteria remain outstanding.
